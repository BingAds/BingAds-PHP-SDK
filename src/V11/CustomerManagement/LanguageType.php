<?php

namespace Microsoft\BingAds\V11\CustomerManagement;

{
    /**
     * Defines a selection of language values.
     * @link https://docs.microsoft.com/en-us/bingads/customer-management-service/languagetype?version=11 LanguageType Value Set
     * 
     * @used-by Account
     * @used-by Customer
     */
    final class LanguageType
    {
        /** The corresponding language type. */
        const Danish = 'Danish';

        /** The corresponding language type. */
        const Dutch = 'Dutch';

        /** The corresponding language type. */
        const English = 'English';

        /** The corresponding language type. */
        const Finnish = 'Finnish';

        /** The corresponding language type. */
        const French = 'French';

        /** The corresponding language type. */
        const German = 'German';

        /** The corresponding language type. */
        const Italian = 'Italian';

        /** The corresponding language type. */
        const Japanese = 'Japanese';

        /** The corresponding language type. */
        const Norwegian = 'Norwegian';

        /** The corresponding language type. */
        const Portuguese = 'Portuguese';

        /** The corresponding language type. */
        const Swedish = 'Swedish';

        /** The corresponding language type. */
        const Spanish = 'Spanish';

        /** The corresponding language type. */
        const Arabic = 'Arabic';

        /** The corresponding language type. */
        const Hebrew = 'Hebrew';

        /** The corresponding language type. */
        const Korean = 'Korean';

        /** The corresponding language type. */
        const Russian = 'Russian';

        /** The corresponding language type. */
        const TraditionalChinese = 'TraditionalChinese';

        /** The corresponding language type. */
        const SimplifiedChinese = 'SimplifiedChinese';
    }

}
